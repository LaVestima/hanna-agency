{% extends 'base.html.twig' %}

{% block content %}
    {% if pagination | length > 0 or not isAsync %}
        {{ block('entityList') }}
    {% else %}
        No Items!
    {% endif %}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('list') }}
{% endblock %}
