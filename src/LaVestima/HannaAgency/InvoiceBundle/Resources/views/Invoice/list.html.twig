{% extends '@Infrastructure/base.html.twig' %}

{% block content %}
    invoice list...
    {% for invoice in invoices %}
        {{ invoice.name }}
        {{ invoice.user.login }}
    {% endfor %}
{% endblock %}