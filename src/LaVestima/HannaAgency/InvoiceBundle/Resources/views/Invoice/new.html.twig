{% extends '@Infrastructure/base.html.twig' %}

{% block title %}New invoice{% endblock %}

{% block content %}
    new invoice...
    <br><br><br><br>
    dropdown with orders + their statuses (can only issue invoice when order completed)
    {{ form_start(form) }}
        <table>
            <tr>
                <td>
                    {{ form_label(form['name']) }}
                </td>
                <td>
                    {{ form_widget(form['name']) }}
                </td>
            </tr>
        </table>
    {{ form_end(form) }}
{% endblock %}