{% extends '@Infrastructure/base.html.twig' %}

{% block content %}
    {% if pagination | length > 0 %}
        {{ block('entityList') }}
    {% else %}
        No Items!
    {% endif %}
{% endblock %}
